var { assert, expect } = require("chai");

var { Shop, Item } = require("../gilded_rose.js");

describe("Gilded Rose", function () {
  it("should test", function () {
    assert.isOk('everyting is okay');
  });
});
